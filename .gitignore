# Rust
/agentd/target/
**/*.rs.bk
# Cargo.lock is tracked — agentd is a binary workspace (reproducible builds matter)

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Runtime event log (append-only JSONL, not source)
agentd/events/

# Secrets / local config overrides
*.local.toml
.env
.claude/
